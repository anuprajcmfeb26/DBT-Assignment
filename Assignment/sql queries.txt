
4. SELECT ....................
7:17:15 pm -> ? is this Time stored internally 

mysql> SELECT DATE_FORMAT(NOW(), '%h:%i:%s %p');
+-----------------------------------+
| DATE_FORMAT(NOW(), '%h:%i:%s %p') |
+-----------------------------------+
| 12:02:57 PM                       |
+-----------------------------------+

5. SELECT ....... sysdate()
Add 1 year 3 months 17 days 10 hours 12 minutes 20 seconds

mysql> SELECT NOW()
    ->        + INTERVAL 1 YEAR
    ->        + INTERVAL 3 MONTH
    ->        + INTERVAL 17 DAY
    ->        + INTERVAL 10 HOUR
    ->        + INTERVAL 12 MINUTE
    ->        + INTERVAL 20 SECOND;
+---------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| NOW()
       + INTERVAL 1 YEAR
       + INTERVAL 3 MONTH
       + INTERVAL 17 DAY
       + INTERVAL 10 HOUR
       + INTERVAL 12 MINUTE
       + INTERVAL 20 SECOND |
+---------------------------------------------------------------------------------------------------------------------------------------------------------------------+
| 2027-06-30 23:03:56                                                                                                                                                 |
+---------------------------------------------------------------------------------------------------------------------------------------------------------------------+
1 row in set (0.00 sec)

6. Write a SELECT statement to display today's date and time:-

Today is Thu, 12th Mar, 2026, and the Time is 7:50:21 pm

mysql> SELECT CONCAT(
    -> 'Today is ',
    -> DATE_FORMAT(NOW(), '%a, %D %b, %Y'),
    -> ', and the Time is ',
    -> LOWER(DATE_FORMAT(NOW(), '%h:%i:%s %p'))
    -> );
+----------------------------------------------------------------------------------------------------------------------------+
| CONCAT(
'Today is ',
DATE_FORMAT(NOW(), '%a, %D %b, %Y'),
', and the Time is ',
LOWER(DATE_FORMAT(NOW(), '%h:%i:%s %p'))
) |
+----------------------------------------------------------------------------------------------------------------------------+
| Today is Fri, 13th Mar, 2026, and the Time is 12:54:59 pm                                                                  |
+----------------------------------------------------------------------------------------------------------------------------+
1 row in set (0.00 sec)